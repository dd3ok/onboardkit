{
  "criteria": [
    {
      "id": "C01",
      "description": "Run the focused verification command for {{title}}",
      "type": "command",
      "command": "npm test",
      "required": true
    },
    {
      "id": "C02",
      "description": "Run a syntax or lint check if available",
      "type": "command",
      "command": "npm run lint:syntax",
      "required": false
    }
  ]
}
