---
alwaysApply: true
---

# after-init

Use `AGENTS.md` as the canonical project guidance. This Cursor rule is a thin compatibility shim and should not duplicate long procedures.

Use the docs index in `AGENTS.md` when project source-of-truth priority is unclear.
