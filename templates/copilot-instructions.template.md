# Copilot Instructions

Use `AGENTS.md` as the canonical repository guidance for coding-agent work.

Keep this file as a thin pointer. Do not duplicate rules from `AGENTS.md`; update the canonical guidance instead.
