interface:
  display_name: "onboardkit"
  short_description: "Maintain minimal, evidence-based AGENTS.md guidance."
  default_prompt: "Use $onboardkit to keep only durable agent actions; put evidence gaps and conflicts in the report and verify read-only."
policy:
  allow_implicit_invocation: true
