interface:
  display_name: "onboardkit"
  short_description: "Maintain minimal, evidence-based AGENTS.md guidance."
  default_prompt: "Use $onboardkit to create only durable, actionable AGENTS.md guidance for this repo."
policy:
  allow_implicit_invocation: true
