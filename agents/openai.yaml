interface:
  display_name: "onboardkit"
  short_description: "Maintain scoped, evidence-based AGENTS.md guidance."
  default_prompt: "Use $onboardkit to maintain only durable, scoped agent guidance; keep evidence gaps and conflicts in the report and verify read-only."
policy:
  allow_implicit_invocation: true
