interface:
  display_name: "security-review"
  short_description: "Review agent-produced changes for security, secret handling, network access, authz/authn, "
policy:
  allow_implicit_invocation: true
