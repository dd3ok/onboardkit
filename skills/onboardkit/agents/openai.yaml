interface:
  display_name: "onboardkit"
  short_description: "Initialize and maintain lightweight AGENTS.md and docs."
  default_prompt: "Use $onboardkit to initialize lightweight AGENTS.md and docs routing for this repo."
policy:
  allow_implicit_invocation: true
