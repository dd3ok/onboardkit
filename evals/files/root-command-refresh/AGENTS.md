# Repository guidance

- Never commit `.env` files.
- Read `docs/testing.md` before changing test infrastructure.
- Run `npm test` before submitting changes.
- Verify documentation-only changes with `git diff --check`.
